{
  "name": "@aunited/tinkoff-api",
  "version": "0.0.1-pre-alpha.0",
  "main": "lib/index.js",
  "license": "UNLICENSED",
  "files": [
    "lib"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "yarn run lint && yarn run typecheck && yarn run format"
    }
  },
  "scripts": {
    "commit": "mctl commit",
    "lint": "mctl lint",
    "typecheck": "mctl typecheck",
    "format": "mctl format",
    "build": "tsc -p ./tsconfig.json",
    "watch:build": "tsc --watch",
    "prepublishOnly": "yarn run format && yarn run lint && yarn run typecheck",
    "prepack": "yarn run build"
  },
  "dependencies": {
    "node-fetch": "2.6.0"
  },
  "devDependencies": {
    "@monstrs/mctl": "0.1.82",
    "@types/node": "13.7.4",
    "@types/node-fetch": "2.5.4",
    "husky": "4.2.3",
    "typescript": "3.8.2"
  }
}
