{
  "name": "@aunited/tinkoff-api",
  "version": "0.0.1-pre-alpha.6",
  "main": "lib/index.js",
  "license": "UNLICENSED",
  "files": [
    "lib"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "yarn run lint && yarn run typecheck && yarn run format && yarn run test"
    }
  },
  "scripts": {
    "test": "mctl test",
    "commit": "mctl commit",
    "lint": "mctl lint",
    "typecheck": "mctl typecheck",
    "format": "mctl format",
    "build": "tsc -p ./tsconfig.json",
    "watch:build": "tsc --watch",
    "prepublishOnly": "yarn run format && yarn run lint && yarn run typecheck && yarn run test",
    "prepack": "yarn run build"
  },
  "dependencies": {
    "node-fetch": "2.6.1"
  },
  "devDependencies": {
    "@monstrs/mctl": "0.1.82",
    "@types/node": "13.13.11",
    "@types/node-fetch": "2.5.7",
    "husky": "4.2.5",
    "jest": "26.5.3",
    "typescript": "3.9.6"
  }
}
